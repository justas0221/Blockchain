#include "hash.h"
#include "blockchain.h"

int main()
{
    std::ifstream users("users.txt");
    std::ifstream transactions("transactions.txt");

    

    users.close();
    transactions.close();

    return 0;
}